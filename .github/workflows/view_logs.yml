name: Deploy

on:
  push:
  workflow_dispatch:

jobs:
  evaluate:
    runs-on: ubuntu-latest
    outputs:
      proceed: ${{ steps.set-var.outputs.proceed }}
    steps:
      - name: Set proceed flag
        id: set-var
        run: |
          echo "proceed=true" >> $GITHUB_OUTPUT  # Change to true or false as needed

  approve:
    runs-on: ubuntu-latest
    needs: evaluate
    if: needs.evaluate.outputs.proceed == 'false'
    environment:
      name: manual-approval
    outputs:
      approved: ${{ steps.set-output.outputs.approved }}
    steps:
      - name: Wait for manual approval
        id: set-output
        run: |
          echo "approved=true" >> $GITHUB_OUTPUT

  deploy:
    runs-on: ubuntu-latest
    needs: evaluate    # only depend on evaluate job
    # No if condition here, always run deploy job
    steps:
      - name: Deploy step
        run: echo "🚀 Deploying..."

      - name: Another step
        run: echo "✅ Done!"
