name: Deploy

on:
  push:
  workflow_dispatch:

jobs:
  evaluate:
    runs-on: ubuntu-latest
    outputs:
      proceed: ${{ steps.set-var.outputs.proceed }}
    steps:
      - name: Set proceed flag
        id: set-var
        run: |
          echo "proceed=true" >> $GITHUB_OUTPUT  # 👉 Yahan se toggle karo: true/false

  approve:
    runs-on: ubuntu-latest
    needs: evaluate
    if: needs.evaluate.outputs.proceed == 'false'  # 👈 Only run if false
    environment:
      name: manual-approval
    steps:
      - name: Wait for manual approval
        run: echo "🧍 Waiting for manual approval..."

  deploy:
    runs-on: ubuntu-latest
    needs: [evaluate, approve]
    if: needs.evaluate.outputs.proceed == 'true' || (needs.evaluate.outputs.proceed == 'false' && needs.approve.result == 'success')
    steps:
      - name: Deploy step
        run: echo "🚀 Deploying..."

      - name: Another step
        run: echo "✅ Done!"
